<ul class="nav nav-pills nav-stacked">
        {% for advert in listAdverts %}
        <li>
          <a href="{{ path('tests_platform_view', {'id': advert.id}) }}">
            {{ advert.title }}
          </a>
        </li>
        {% else %}
            <li>Pas d'annonces</li>
        {% endfor %}
</ul>